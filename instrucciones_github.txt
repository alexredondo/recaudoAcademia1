Instrucciones para subir a github:

git add *
git commit -m "Comentario a la actualización"
git remote add origin https://github.com/alexredondo/recaudoAcademia1
git push -u origin master
phrase: 12345

Nota: con git push se actuliza el acceso.

- Para listar los acceso remotos:
git remote -v

- Para iniciar una carpeta git
git init

- Para eliminar una carpeta git
-git rm -rf .git

- Para crear la carpeta git
- got init
